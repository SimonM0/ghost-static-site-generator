const fetchUrlHelper = require('../fetchUrlHelper');

describe.skip('fetchUrlHelper', () => {
  it.skip('should ', () => {
    const expected = fetchUrlHelper();
    const result = true;
    expect(expected).toEqual(result);
  });
});
