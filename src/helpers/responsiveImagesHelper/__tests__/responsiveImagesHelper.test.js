const responsiveImagesHelper = require('../responsiveImagesHelper');

describe.skip('responsiveImagesHelper', () => {
  it.skip('should ', () => {
    const expected = responsiveImagesHelper();
    const result = true;
    expect(expected).toEqual(result);
  });
});
