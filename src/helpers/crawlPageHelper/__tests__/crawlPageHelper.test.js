import crawlPageHelper from '../crawlPageHelper';

describe.skip('crawlPageHelper', () => {
  it.skip('should ', () => {
    const expected = crawlPageHelper();
    const result = true;
    expect(expected).toEqual(result);
  });
});
